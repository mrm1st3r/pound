sudo: required
dist: trusty

language: java
jdk:
  - oraclejdk8
  - oraclejdk9
install:
  - export MAVEN_SKIP_RC=true
  - export MAVEN_OPTS="-Xmx512m -XX:MaxPermSize=512m"
  - "export PATH=`pwd`/bin:$PATH"

script:
  - export DISPLAY=:99.0 && ./mvnw clean package -q
